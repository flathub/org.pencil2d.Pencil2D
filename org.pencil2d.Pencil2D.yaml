app-id: org.pencil2d.Pencil2D
default-branch: stable
runtime: org.kde.Platform
runtime-version: '6.10'
sdk: org.kde.Sdk
command: pencil2d
finish-args:
  - --socket=wayland
  - --share=network # to make the update checker work
  - --share=ipc
  - --socket=fallback-x11
  - --device=dri
  - --socket=pulseaudio
  - --filesystem=host # to make saving work
cleanup:
  - /include
  - /lib/pkgconfig
modules:
  - name: pencil2d
    buildsystem: qmake
    builddir: true
    config-opts:
      - CONFIG+=release
      - CONFIG+=force_debug_info
      - CONFIG+=GIT
      - CONFIG+=PENCIL2D_RELEASE
      - VERSION=0.7.0
    cleanup:
      - /share/bash-completion
      - /share/zsh
    sources:
      - type: git
        url: https://github.com/pencil2d/pencil.git
        commit: 7a0918f8baca1a090c1d58b29b13c70e11d564bf
        tag: v0.7.0
      - type: patch
        path: qt-6.9.patch
      - type: patch
        path: qt-6.10.1.patch
